---
title: 智能周末闲时规划助手设计风格
description: 当前实现版本设计规范（iPhone 16 393×852），强调首页背景渐变一致性
author: MEITUAN Hackathon Team
ms.date: 2026-06-06
ms.topic: reference
keywords:
  - 设计规范
  - iOS
  - iPhone 16
  - 周末规划
estimated_reading_time: 10
---

## 1. 设计定位与平台

产品定位为智能周末闲时规划助手，核心目标是帮助用户在碎片化空闲时间内，用更低决策成本完成从想法到出行的闭环。

平台基准为 iPhone 16 竖屏原型，设计尺寸固定为 393 × 852。

当前版本关键词：温暖、松弛、轻决策、城市生活感。

## 2. 全局视觉基线

全局背景底色统一为 #F9F9F9。

文本和功能色保持简洁：

* 主文字色 #000000
* 状态文字色 #6E6E6E
* 主要深色块 #2A2A2A
* 行为强调色 #461C3A（用于登录按钮）

圆角建议：

* 屏幕容器 47px
* 卡片 24px 到 28px
* 胶囊控件和输入条 9999px

## 3. 背景渐变规范

### 3.1 一致性要求

首页1和首页2必须使用完全一致的背景渐变语言，不允许其中一页出现单独的色块配方。

允许差异仅限于内容区布局和文案，不包含背景渐变体系本身。

### 3.2 渐变构成

背景由 4 个柔焦色团构成，叠加在 #F9F9F9 底色上：

* 玫瑰色团：rgba(253, 202, 206, 0.9)
* 金色团：rgba(255, 249, 142, 0.95)
* 象牙色团：rgba(255, 255, 255, 0.88)
* 薄雾紫团：rgba(215, 164, 224, 0.42)

统一效果要求：

* 使用大圆角并带模糊，形成软边扩散
* 视觉重心位于屏幕下半区
* 不出现硬边、色带、竖向白条或块状断层

## 4. 首页1规范

首页1为启动页，强调品牌识别和轻量引导。

布局重点：

* 顶部为 iOS 风格状态栏和动态岛
* 中部为 Logo + Weekendgo 标题
* 底部为一句品牌说明文字
* 点击任意区域进入首页2

视觉要求：

* 层级简洁，不堆叠多余组件
* 保持大留白，避免信息拥挤

## 5. 首页2规范

首页2为登录页，承接首页1视觉语气，背景渐变与首页1完全一致。

布局重点：

* 顶部保留同样的状态栏结构
* 中上区域为品牌标识和引导文案
* 中部为昵称、手机号、密码三项输入区
* 登录按钮作为主行动作
* 下方提供第三方登录入口

视觉要求：

* 输入区采用半透明浅色胶囊
* 登录按钮使用深色高对比样式
* 分割线与辅助文本保持低对比，不抢主流程注意力

## 6. 首页1与首页2统一规则

以下项目必须严格统一：

* 背景底色
* 背景渐变配方
* 状态栏视觉样式
* Home Indicator 呈现方式

以下项目允许按页面目标变化：

* 主内容模块
* 文案和按钮
* 表单和社交登录组件

## 7. 底部导航定义

底部导航从左到右固定为以下 5 个按钮：

* 个人
* 发现
* AI
* 行程
* 聊天

交互要求如下：

* 第 3 个按钮“AI”用于跳转到 AI 生成页
* 第 4 个按钮“行程”用于跳转到行程页
* AI 生成页中，第 3 个按钮为当前激活态
* 行程页中，第 4 个按钮为当前激活态

## 8. 实现检查清单

联调时按以下清单验收：

* 首页1和首页2在空白区域的色调过渡是否一致
* 两页是否都没有边缘白条、灰块或突兀断层
* 状态栏和动态岛位置是否对齐
* 登录流程跳转是否保持稳定
* 页面在 393 × 852 基准下是否无裁切异常
