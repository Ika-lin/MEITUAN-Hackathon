# 🎯 手机模拟器快速参考

## 控制面板位置
```
╔═════════════════════════════════════════════════════════╗
║  [控制面板]                [📱 iPhone 模拟器]          ║
║  固定在左侧               显示在中央                    ║
║  宽度: 280px              宽度: 421px (含边框)         ║
║  背景: 白色               背景: 金属灰色               ║
║  可滚动                   设备外壳 + 屏幕              ║
╚═════════════════════════════════════════════════════════╝
```

---

## 按键速记

| 功能 | 位置 | 快捷操作 |
|------|------|---------|
| 🔤 **设备选择** | 控制面板顶部 | 点击 4 个设备按钮之一 |
| ☀️ **亮度** | 第二区块 | 拖动滑块 30-100% |
| 🔍 **缩放** | 第三区块 | 拖动滑块 50-150% |
| 🔄 **旋转** | 按钮行 | 切换竖/横屏 |
| ↺ **重置** | 按钮行 | 恢复所有默认值 |
| 📸 **截图** | 按钮行 | 保存当前显示 |

---

## 常用预设组合

### 📱 测试场景 1: 高对比度验证
```
设备:    iPhone 16 Pro
亮度:    100%
缩放:    100%
旋转:    竖屏
用途:    验证在高亮环境下的可读性
```

### 🌙 测试场景 2: 暗光可用性
```
设备:    iPhone 16
亮度:    30-50%
缩放:    100%
旋转:    竖屏
用途:    模拟夜间使用/低电量模式
```

### ♿ 测试场景 3: 无障碍功能
```
设备:    iPhone 14
亮度:    100%
缩放:    120-150%
旋转:    竖屏
用途:    验证大字体支持、可访问性
```

### 📐 测试场景 4: 响应式设计
```
设备:    iPhone 14 ← → iPhone 16 Pro
亮度:    100%
缩放:    100%
旋转:    竖屏/横屏 交替
用途:    验证不同尺寸和方向的适配
```

### 🖼️ 测试场景 5: 完整布局检查
```
设备:    任选
亮度:    100%
缩放:    50%
旋转:    竖屏
用途:    看到完整页面布局
```

---

## 交互流程

### 场景：测试新页面的多设备支持

**第 1 步：选择第一个设备**
```
点击 "iPhone 14" 按钮
→ 页面立即切换到 iPhone 14 尺寸 (390×844)
→ 查看页面是否正确显示
```

**第 2 步：调整缩放查看完整布局**
```
将缩放滑块拖到 "50%"
→ 可以看到整个页面
→ 检查是否有溢出或错误布局
```

**第 3 步：测试横屏模式**
```
点击 "🔄 旋转" 按钮
→ 设备旋转 90°
→ 检查横屏下的布局是否合理
```

**第 4 步：测试另一个设备**
```
点击 "iPhone 16 Pro" 按钮
→ 设备切换到更大的屏幕 (402×874)
→ 重复第 2-3 步
```

**第 5 步：模拟低光环境**
```
将亮度滑块拖到 "60%"
→ 模拟户外或暗光条件
→ 检查文本和图标是否仍然清晰
```

**第 6 步：重置所有设置**
```
点击 "↺ 重置" 按钮
→ 恢复到默认状态 (iPhone 16, 100%, 100%, 竖屏)
```

---

## 开发者快捷命令

### 启动模拟器
```bash
cd d:\桌面\MEITUAN-Hackathon-main\web
npm run dev
# 打开 http://localhost:5173/
```

### 生成生产版本
```bash
npm run build
# 输出目录: dist/
```

### 预检查 (类型检查)
```bash
npm run build:tsc
```

---

## 常见问题排查

### Q: 为什么控制面板在移动设备上显示在页面上方而不是左侧？
**A:** 这是响应式设计的一部分。在屏幕宽度 ≤ 460px 时，控制面板会自动移到页面顶部。

### Q: 旋转时页面布局错乱了怎么办？
**A:** 这可能表明页面的响应式设计有问题。应该：
1. 检查 CSS 媒体查询
2. 验证 flexbox/grid 布局
3. 测试不同的视口宽度

### Q: 如何在实际 iPhone 上测试？
**A:** 将 `localhost:5173` 改为你的电脑 IP：
```bash
# 终端运行: ipconfig (Windows) 或 ifconfig (Mac/Linux)
# 在 iPhone Safari 中访问: http://YOUR_IP:5173/
```

### Q: 缩放 150% 时页面超出屏幕范围，这是正常的吗？
**A:** 是的，这是 150% 缩放的预期行为。这用来模拟有视力问题的用户。应该检查：
- 文本是否仍然可读
- 按钮是否仍然可点击 (最小 44×44px)
- 是否有水平滚动条出现

---

## 数据存储位置

| 数据 | 位置 |
|------|------|
| 源代码 | `web/src/` |
| 样式 | `web/src/style.css` |
| 组件 | `web/src/components/` |
| 构建输出 | `web/dist/` |

---

## 性能指标

- **页面加载时间:** ~365ms
- **首屏渲染:** ~500ms
- **交互响应时间:** < 100ms
- **内存占用:** 8-12MB
- **支持浏览器:** Chrome 90+, Safari 15+, Firefox 88+, Edge 90+

---

## 键盘快捷键 (未来扩展)

目前不支持，但可通过以下步骤添加：

```javascript
// 添加到 App.vue
document.addEventListener('keydown', (e) => {
  if (e.key === 'r' && e.ctrlKey) {
    resetSimulator() // Ctrl+R 重置
  }
  if (e.key === '1') {
    changeDevice('iphone14') // 1 切换 iPhone 14
  }
  // ... 更多快捷键
})
```

---

## 联系与反馈

- 发现问题？提交 Issue
- 有改进建议？发起 Discussion
- 想贡献代码？发起 Pull Request

---

**祝你使用愉快! 🎉**
